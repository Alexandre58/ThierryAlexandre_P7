const initialState = {
    uid: "user id",
    userName: "My name",
    userEmail: "ntjoel19@gmail.com",
  };
  
  const rootReducer = (state = initialState, action) => {
    return state;
  };
  
  export default rootReducer;